{
    "title": "User",
    "description": "A user profile with a name, email address, and age.",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "description": "The user's full name."
        },
        "email": {
            "type": "string",
            "format": "email",
            "description": "The user's email address."
        },
        "age": {
            "type": "integer",
            "minimum": 0,
            "description": "The user's age in years."
        }
    },
    "required": [
        "name",
        "email",
        "age"
    ],
    "additionalProperties": false
}